enum CoreEnumTheme { light, dark, contrast }

enum CoreEnumDevice { mobile, tablet, desktop, television, foldable, watch }

enum CoreEnumOrientation {
  portrait,
  landscape,
}

enum CoreEnumScheme {
  http,
  https
}

enum CoreEnumSize { xxs, xs, s, m, l, xl, xxl }

enum CoreEnumFile {
  avi,
  bmp,
  css,
  docx,
  env,
  gif,
  html,
  jpeg,
  jpg,
  js,
  json,
  md,
  mov,
  mp3,
  mp4,
  pdf,
  png,
  ppt,
  rar,
  svg,
  tiff,
  txt,
  wav,
  xls,
  xml,
  zip
}